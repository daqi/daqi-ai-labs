# Workspace Conventions

- Use the root `.venv/`; create it if needed, and do not create other virtual environments.
- Keep temporary scripts in `/tmp` unless the user explicitly wants them in the repository.